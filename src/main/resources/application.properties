spring.application.name=helloDocs
server.servlet.context-path=/api

# Database Configuration
spring.datasource.url=jdbc:postgresql://localhost:5432/flashcard_db
spring.datasource.username=postgres
spring.datasource.password=chidera
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA Configuration
spring.jpa.hibernate.ddl-auto=create
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true
spring.sql.init.mode=never
# JWT Configuration (MISSING - add this)
jwt.secret=099e5714c85c3b1aeaed0aba1fff5912
jwt.expiration=86400000

# CORS Configuration (FIXED - remove extra colon)
cors.allowed-origins=http://localhost:19006,http://localhost:8081,exp://localhost:19000